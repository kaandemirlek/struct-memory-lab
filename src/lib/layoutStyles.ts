// Shared visual constants for the memory-layout views.
// The stripe color is a theme-aware token (--hatch in globals.css) so padding
// reads correctly on both the dark and light surfaces.
export const HATCH =
  "repeating-linear-gradient(45deg, var(--hatch) 0 4px, transparent 4px 8px)";
